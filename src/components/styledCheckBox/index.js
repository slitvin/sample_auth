import React from "react";
import { CustomInput, FormGroup, Label, Input } from "reactstrap";

export default function StyledCheckBox({ title, ...rest }) {

  return (
    <>
      <FormGroup check>
      </FormGroup>
    </>
  );
}
